# Protect sensitive files
<FilesMatch "users.json|error.log">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Optional: you can add more file protections here
# <FilesMatch "\.(env|ini|json)$">
#     Order Allow,Deny
#     Deny from all
# </FilesMatch>
